{% set language = fsc.getMainModel() %}

<div class="container">
    <div class="columns">
        <div class="column col-2 col-sm-12 text-center">
            <i class="fas fa-language fa-4x fa-fw" aria-hidden="true"></i>
        </div>
        <div class="column">
            <h1 class="h4">
                {{ language.langcode }}
            </h1>
            <p>
                {{ language.description }}
                {% if language.idcontacto %}
                    <br/><b>{{ i18n.trans('mantainer') }}</b>:
                    <a href="{{ asset(language.getContactProfile()) }}">{{ language.getContactAlias() }}</a>
                {% endif %}
            </p>
        </div>
    </div>
</div>
<br/>